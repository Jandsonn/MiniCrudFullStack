<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Editar tarefas</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
    ></script>
  </head>
  <body>

    <h1>Editar a tarefa: {{task.title}}</h1>
    <div class="container d-flex justify-content-center">
      <img src="https://img.icons8.com/doodle/48/000000/document.png"/>
      <form action="/tasks/add" method="POST">
      <input type="hidden" name="id" value{{this.id}}>
        <div class="mb-4 mt-5">
          <label for="title">Título:
          <input type="text" class="form-control" name="title"
           value="{{task.title}}" style="border-color:#FFCA29" 
           placeholder="O que você vai fazer ?"/>
          </label>
        </div>
        <div>
        <label for="description">Comments:
        <textarea class="form-control" style="border-color:#FFCA29;"
         rows="5" name="description">{{task.description}}</textarea>
        </label>
        <br/>
        <input type="submit" value="Editar" class="btn btn-warning"></input>
      </form>

      <!--Observação, no tag label será preciso  ter o nome do dado do banco, e após isso ter um campo name onde é a entrada de texto com o nome da variável 
        exemplo: for="description" < na label, e name="description na textarea ou em algum tipo de input"
      -->
    </div>
  </body>
</html>